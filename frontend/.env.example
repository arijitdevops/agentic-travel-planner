# Leave empty to call the API through the Vite dev proxy (/api -> VITE_PROXY_TARGET).
# Set to e.g. http://localhost:8000 to call the backend directly (CORS must allow it).
VITE_API_BASE_URL=
VITE_PROXY_TARGET=http://localhost:8000
